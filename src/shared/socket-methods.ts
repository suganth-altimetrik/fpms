// import socket from './socket';

// export const joinRoom = (room_id: string) => socket.emit('join_room', room_id);

// export const getSocketMessage = (endpoint: string, callback: any) =>
//   socket.on(endpoint, (msg: any) => callback(msg));
